/**
 * # Description
 * Get the current time in milliseconds.
 * 
 * # Returns
 * time_now (int<<12>>): current time in milliseconds
 **/
fun int<<12>> get_time_now_ms () {
    int time_now = Sys.time();
    return time_now[31:20]; // TODO: Double check slicing
}

